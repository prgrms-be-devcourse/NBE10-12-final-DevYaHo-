logging:
  level:
    com.wellbuying: DEBUG

jwt:
  # 로컬은 http://localhost라 Secure 쿠키는 브라우저가 저장하지 않는다
  cookie-secure: false

aws:
  s3:
    # docker-compose.yml의 minio 서비스(로컬 전용 고정 계정) - 운영 자격증명과 무관
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
